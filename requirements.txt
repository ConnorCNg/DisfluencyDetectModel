# Apple Silicon: if imports fail with wrong arch, reinstall for arm64, e.g.
#   arch -arm64 python -m pip install --force-reinstall numpy torch ...

# Shared / download_audio.py / extract_clips.py / disfluency_pipeline.py
numpy>=1.26
pandas>=1.3
scipy>=1.7

# extract_clips.py (--progress)
tqdm>=4.60

# disfluency_pipeline.py (wav2vec2, Whisper, MFCC, training, F1 metrics)
torch>=2.0
torchaudio>=2.0
transformers>=4.30,<5
soundfile>=0.12
scikit-learn>=1.0
